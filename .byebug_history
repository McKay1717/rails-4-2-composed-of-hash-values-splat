assert_raises ArgumentError
assert_raises ArgumentError do
    end
      user.preferences = user.preferences.to_hash.merge({ newsletters: false }) 
assert_raises ArgumentError do
proc { user.preferences = user.preferences.to_hash.merge({ newsletters: false }) }.must_raise ArgumentError.new("wrong number of arguments (given 3, expected 1)")
user.preferences = user.preferences.to_hash.merge({ newsletters: false })
user.preferences.to_hash.merge({ newsletters: false })
user.preferences.to_hash
user.preferences
user = User.new
exit
user.preferences
user = User.new
require "user/preferences"
require "user/preferences.rb
require_relative "user/preferences.rb
    user.preferences
user = User.new
u = User.new
User.new
User
self
